import React from 'react'

function Rules() {
    return (
        <div className="rules">
            <button className="rules__btn">
                RULES
            </button>
        </div>
    )
}

export default Rules
